name: "Build"
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest 
    steps:
      - uses: actions/checkout@v4
      - uses: setup-node@v4
        with:
          node-version: 22
      - run: |
            cd ./webpage
            npm ci
            npm run build
